import React from 'react';
import { NavLink } from 'react-router-dom';


const Header = () => (
    <div className="widget-header">
        <h1 className="widget-header--title">Wheather</h1>
    </div>
);

export default Header;